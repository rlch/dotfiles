# Environment files — never track. Templates (.example, .sample, .template)
# are explicitly allowed back so dot_env.example stays trackable.

# Literal .env at any depth
.env
.env.*
!.env.example
!.env.sample
!.env.template

# chezmoi source names (dot_env → ~/.env on apply)
dot_env
dot_env.*
!dot_env.example
!dot_env.sample
!dot_env.template
