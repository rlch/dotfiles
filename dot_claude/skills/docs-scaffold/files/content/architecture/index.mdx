---
title: Architecture
type: architecture
status: draft
tags: [architecture, overview]
---

# Architecture

Describe the system at a high level: the major components, how data/control
flows between them, and where the boundaries are. Keep this *descriptive* —
prescriptions and trade-offs belong in [decisions](/docs/decisions).

## Components

| Component | Role | Talks to |
| --------- | ---- | -------- |
| _example_ | _what it owns_ | _its neighbours_ |

## How the parts interact

Diagrams are fenced ` ```d2 ` blocks — edit the text, the SVG regenerates at
build time. Example:

```d2
direction: right

client: "Client"
api: "API" {
  shape: hexagon
}
db: "Store" {
  shape: cylinder
}

client -> api: request
api -> db: read/write
api -> client: response
```

## See also

- [Decisions](/docs/decisions) — why it's shaped this way
- `openspec/specs/` — the normative behaviour contracts
