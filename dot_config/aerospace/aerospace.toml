start-at-login = true
on-focus-changed = ['move-mouse window-lazy-center']
automatically-unhide-macos-hidden-apps = true

# Reap ghost windows left behind by AeroSpace bug #1615 (macOS Tahoe stops
# delivering window-close events, so closed windows linger as empty-title
# nodes that slice the layout). reap-ghosts.sh closes them, debounced + with
# the focused window skipped + an app exclude-list (Gather is title-less when
# unfocused) so it never closes a real window. Backgrounded (`&`) so it never
# delays the workspace switch.
exec-on-workspace-change = ['/bin/bash', '-c',
    '~/.config/aerospace/reap-ghosts.sh >/dev/null 2>&1 &']

enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

accordion-padding = 30
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'

[key-mapping]
preset = 'qwerty'

[gaps]
inner.horizontal = 10
inner.vertical = 10
outer.left = 10
outer.right = 10
outer.top = 10
outer.bottom = 10

[mode.main.binding]
# layouts
alt-slash      = 'layout tiles horizontal vertical'
alt-comma      = 'layout accordion horizontal vertical'
alt-f          = 'fullscreen'
ctrl-alt-f     = 'macos-native-fullscreen'
alt-shift-f    = 'layout floating tiling'

# focus + move (vim-style, cross-monitor)
alt-h = 'focus left  --boundaries all-monitors-outer-frame'
alt-j = 'focus down  --boundaries all-monitors-outer-frame'
alt-k = 'focus up    --boundaries all-monitors-outer-frame'
alt-l = 'focus right --boundaries all-monitors-outer-frame'
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# resize
alt-minus = 'resize smart -100'
alt-equal = 'resize smart +100'

# workspaces — named roles. Aerospace's tray icon surfaces the active
# workspace name in the menu bar, so digits are uninformative. Cycle order
# (alt-n/p) is lexical; direct jumps via ctrl-alt-N still hit the right
# workspace regardless of cycle position.
ctrl-alt-1 = 'workspace code'
ctrl-alt-2 = 'workspace browser'
ctrl-alt-3 = 'workspace notes'
ctrl-alt-4 = 'workspace comms'
ctrl-alt-5 = 'workspace notion'
ctrl-alt-6 = 'workspace dev'
ctrl-alt-7 = 'workspace trading'
# alt-n/p cycle through workspaces ON THE FOCUSED MONITOR ONLY, in the
# explicit order defined by ~/.config/aerospace/workspace_order.sh. The
# focused_workspaces.sh helper filters the canonical order down to the
# focused monitor's workspaces; --wrap-around is intentionally OFF so
# pressing alt-n at the last workspace of the monitor does nothing
# (rather than jumping to a workspace on a different monitor).
alt-n       = 'exec-and-forget ~/.config/aerospace/focused_workspaces.sh | aerospace workspace next --stdin'
alt-p       = 'exec-and-forget ~/.config/aerospace/focused_workspaces.sh | aerospace workspace prev --stdin'
alt-tab     = 'workspace-back-and-forth'
alt-shift-n = 'exec-and-forget ~/.config/aerospace/focused_workspaces.sh | aerospace move-node-to-workspace next --stdin --focus-follows-window'
alt-shift-p = 'exec-and-forget ~/.config/aerospace/focused_workspaces.sh | aerospace move-node-to-workspace prev --stdin --focus-follows-window'

# move window to workspace (mirrors ctrl-alt-N workspace-switch chord)
ctrl-alt-shift-1 = 'move-node-to-workspace code --focus-follows-window'
ctrl-alt-shift-2 = 'move-node-to-workspace browser --focus-follows-window'
ctrl-alt-shift-3 = 'move-node-to-workspace notes --focus-follows-window'
ctrl-alt-shift-4 = 'move-node-to-workspace comms --focus-follows-window'
ctrl-alt-shift-5 = 'move-node-to-workspace notion --focus-follows-window'
ctrl-alt-shift-6 = 'move-node-to-workspace dev --focus-follows-window'
ctrl-alt-shift-7 = 'move-node-to-workspace trading --focus-follows-window'

# focus monitors directly
alt-1 = 'focus-monitor 1'
alt-2 = 'focus-monitor 2'
alt-3 = 'focus-monitor 3'
alt-4 = 'focus-monitor 4'

# move window to monitor (mirrors alt-N focus-monitor chord)
alt-shift-1 = 'move-node-to-monitor 1 --focus-follows-window'
alt-shift-2 = 'move-node-to-monitor 2 --focus-follows-window'
alt-shift-3 = 'move-node-to-monitor 3 --focus-follows-window'
alt-shift-4 = 'move-node-to-monitor 4 --focus-follows-window'

# enter service mode (now properly scoped, unlike old config)
alt-shift-semicolon = 'mode service'

[mode.service.binding]
esc       = ['reload-config', 'mode main']
r         = ['flatten-workspace-tree', 'mode main']
backspace = ['close-all-windows-but-current', 'mode main']
f         = ['layout floating tiling', 'mode main']
alt-shift-h = ['join-with left',  'mode main']
alt-shift-j = ['join-with down',  'mode main']
alt-shift-k = ['join-with up',    'mode main']
alt-shift-l = ['join-with right', 'mode main']

# workspace → monitor (with graceful fallback when monitors absent)
[workspace-to-monitor-force-assignment]
# Match by monitor *name regex* (not index) so the same config rides
# across home/office without renumbering. Each entry is a fallback chain:
# the first pattern that matches a connected monitor wins; "main" is the
# last-resort fallback (the macOS-designated primary in Displays).
#
# Home setup (3 monitors):    DELL S2721DGF, HP 25x, Built-in Retina Display
# Office setup (3 monitors):  L27e-40 (1), L27e-40 (2), Built-in Retina Display
#   The two Lenovos are identical, so aerospace disambiguates them with
#   `(1)` / `(2)` suffixes derived from macOS's display-arrangement order.
#   If the physical layout feels swapped (e.g. you sit looking at "(2)"
#   when typing in `code`), swap the (1)/(2) digits below — no other
#   change needed.
#
# The chain fall-through naturally encodes "home gets X, office gets Y":
# list the home-only monitor first when home should win, list the
# office-only monitor first when office should win. The other site's
# monitor is absent at runtime so the chain falls past it.

# Primary working surface. At home this is the big DELL landscape; at
# the office it collapses onto the laptop (centered between the two
# L27es, so Built-in plays the primary role).
code    = ['DELL S2721DGF', 'Built-in.*', 'main']

# Secondary external — browser + Flutter dev (Chrome). Home: HP 25x.
# Office: L27e-40 (1), the more prominent of the two Lenovos.
browser = ['HP 25x', 'L27e-40 \(1\)', 'main']
dev     = ['HP 25x', 'L27e-40 \(1\)', 'main']

# Peripheral — chat / notes / notion. Home: laptop screen. Office:
# L27e-40 (2), the off-side Lenovo. Office takes priority in the chain
# because at home L27e-40 is absent, so it falls through to Built-in.
comms   = ['L27e-40 \(2\)', 'Built-in.*', 'main']
notion  = ['L27e-40 \(2\)', 'Built-in.*', 'main']
notes   = ['L27e-40 \(2\)', 'Built-in.*', 'main']

# Trading — MooMoo lives on the secondary external (same chain as
# browser/dev). Home: HP 25x. Office: L27e-40 (1).
trading = ['HP 25x', 'L27e-40 \(1\)', 'main']

# Game — the Bevy window from `cargo run -p game` (~/dev/grift; the
# repo may be renamed but the workspace stays generic). Pinned to the
# laptop screen exclusively so it never disrupts whichever external
# monitor the user is actively working on. No second-choice external;
# falls through to `main` only when no laptop is connected (which on
# these two hosts is impossible — both have Built-in.*).
game    = ['Built-in.*', 'main']

# Blender — 3D authoring for Curations (~/dev/grift/assets/curations).
# Same pin as `game`: laptop-only so a connected external isn't
# hijacked when Blender opens. Heavy GPU app but the M5 Max + Built-in
# Retina handles it; using the external would still be a single-window
# context-switch hit when alt-tabbing back to code.
blender = ['Built-in.*', 'main']

# persistent app → workspace routing. Focus does not follow — apps land
# in their slot silently, you switch via ctrl-alt-N.
[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
run = 'move-node-to-workspace code'

[[on-window-detected]]
if.app-id = 'org.mozilla.firefox'
run = 'move-node-to-workspace browser'

[[on-window-detected]]
if.app-id = 'md.obsidian'
run = 'move-node-to-workspace notes'

[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = 'move-node-to-workspace comms'

# Gather is a PWA hosted in Brave (NOT Chrome — Chrome PWAs share the
# parent browser's process tree, so quitting Chrome kills them; Brave is
# a separate Chromium with its own lifecycle). Bundle is
# `com.brave.Browser.app.<hash>` and is distinct from base Brave.
# If reinstalling Gather changes the hash, refresh via `aerospace list-apps`.
[[on-window-detected]]
if.app-id = 'com.brave.Browser.app.monjdkojdghbbohnhnccccmkogahhgpj'
run = 'move-node-to-workspace comms'

# Notion (not yet installed; rule fires when its window first appears)
[[on-window-detected]]
if.app-id = 'notion.id'
run = 'move-node-to-workspace notion'

# Chrome → dev. Firefox is the daily-browse browser (workspace browser);
# Chrome is reserved for Flutter web dev, devtools, etc.
[[on-window-detected]]
if.app-id = 'com.google.Chrome'
run = 'move-node-to-workspace dev'

# Flutter native desktop apps. Aerospace can only match by bundle-id /
# app-name / window-title — there's no "links against Flutter framework"
# matcher. Default match: Xcode's stock target name "Runner" (un-renamed
# `flutter create` projects ship with this). For projects you've renamed,
# add a per-bundle-id rule below — find the bundle id with
# `aerospace list-apps` while the app is running.
[[on-window-detected]]
if.app-name-regex-substring = '^Runner$'
run = 'move-node-to-workspace dev'

# Bevy game window → game workspace (pinned to laptop screen above).
# The Bevy binary's macOS app id depends on packaging (debug `cargo run`
# inherits from the parent shell, packaged builds use Info.plist), so
# match by window title which Bevy's `WindowPlugin` sets unconditionally
# to "drift" (crates/game/src/lib.rs). The `^` anchor still matches if a
# window counter is ever appended ("drift (1)"). Update the regex if the
# Bevy `WindowPlugin` title changes. (Was `^grift` before the grift→drift
# repo rename — the stale matcher silently stopped pinning the window,
# leaking it onto whatever external monitor was focused.)
[[on-window-detected]]
if.window-title-regex-substring = '^drift'
run = 'move-node-to-workspace game'

# Blender → blender workspace (pinned to laptop). Match by bundle id;
# Blender's window title varies with the loaded .blend file so it isn't
# a stable matcher.
[[on-window-detected]]
if.app-id = 'org.blenderfoundation.blender'
run = 'move-node-to-workspace blender'

# float-by-default apps
[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'layout floating'

# Neovide opens as a quick popup editor (default handler for text filetypes —
# see Brewfile + post-install-neovide-defaults.sh), so keep it out of the tiles.
[[on-window-detected]]
if.app-id = 'com.neovide.neovide'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.systempreferences'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.ActivityMonitor'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.calculator'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.1password.1password'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.superduper.superwhisper'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'org.pqrs.Karabiner-Elements.Settings'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'org.pqrs.Karabiner-EventViewer'
run = 'layout floating'

